<div class="container h-75 d-flex flex-column justify-content-around">
    <div class="row justify-content-center">
        <div class="col-4">
            <h2 class="mb-4">Login</h2>
            <form action="/login" method="POST">
                <div class="input-group mb-3">
                    <span class="input-group-text"><i class="fa-regular fa-envelope fa-fw"></i></span>
                    <input type="text" name="email" class="form-control" placeholder="Email" value="{{user.email}}" />
                </div>
                <div class="input-group mb-3">
                    <span class="input-group-text"><i class="fa-solid fa-key fa-fw"></i></i></span>
                    <input type="password" name="password" class="form-control {{#if errors.password }}is-invalid{{/if}}" placeholder="Password" />
                    <div class="invalid-feedback mt-2">{{errors.password}}</div>
                </div>
                <div class="d-flex flex-column justify-content-start">
                    <div class="d-flex justify-content-between mb-2">
                        <a href="/register" class="btn btn-link p-0">Crear nuevo usuario</a>
                        <button type="submit" class="btn btn-success">Login</button>
                    </div>
                    <div class="d-flex justify-content-between mt-2">
                        <span class="fst-italic">¿Olvidaste tu contraseña?</span><a href="/login" class="btn btn-link p-0">Recuperar contraseña</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
    <div class="w-100"></div>
</div>